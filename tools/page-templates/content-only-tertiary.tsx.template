'use client';

import {
  AppShellLayout,
  TertiaryColumnLayout,
  PageContentHeader,
  Card,
  Breadcrumb,
  Stack,
  Text,
  Button,
} from '@/components/DesignSystem';

export default function {{PAGE_COMPONENT}}() {
  return (
    <AppShellLayout title="{{PROTOTYPE_TITLE}}" hideNav>
      <Breadcrumb items={[{ label: 'Prototypes', href: '/prototype' }, { label: '{{PAGE_TITLE}}' }]} />

      <PageContentHeader
        title="{{PAGE_TITLE}}"
        subhead="{{PAGE_DESCRIPTION}}"
        badge="Prototype"
      />

      <TertiaryColumnLayout
        main={
          <Card>
            <Stack gap="sm">
              <Text>Replace this with your main content.</Text>
            </Stack>
          </Card>
        }
        aside={
          <Card>
            <Stack gap="sm">
              <Text>Replace this with your aside content.</Text>
              <Button variant="primary" fullWidth>Action</Button>
            </Stack>
          </Card>
        }
      />
    </AppShellLayout>
  );
}
